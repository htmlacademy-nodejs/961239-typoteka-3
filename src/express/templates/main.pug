extends general/layout.pug

block content
  main.main-page
    h1.visually-hidden Главная страница личного блога Типотека
    p.
      
      Это приветственный текст, который владелец блога может выбрать, чтобы описать себя 👏
      
    section.main-page__theme-list
      h2.visually-hidden Список тем
      ul.themes
        li.themes__item
          a.themes__item-link(href='#')
            | Автомобили 
            sup 88
    .main-page__section-flex
      section.main-page__hot.hot
        h2.hot__name
          | Самое обсуждаемое 
          span.hot__icon.hot__icon--fire
        ul.hot__list
          each article in articles
            li.hot__list-item
              a.hot__list-link(href=`/articles/${article.id}`) #{article.title} 
                sup.hot__link-sup 12
      section.main-page__last.last
        h2.last__name
          | Последние комментарии 
          span.last__icon.last__icon--cloud
        ul.last__list
          li.last__list-item
            img.last__list-image(src='/img/avatar-small-1.png' width='20' height='20' alt='Аватар пользователя')
            b.last__list-name Анна Артамонова
            a.last__list-link(href='#') Сервис аренды жилья Airbnb стал глобальным партнером Международного олимпийского комитета (МОК) на девять лет, в течение которых пройдет пять Олимпиад, в том числе в Токио в 2020 году.
          li.last__list-item
            img.last__list-image(src='/img/avatar-small-2.png' width='20' height='20' alt='Аватар пользователя')
            b.last__list-name Александр Петров
            a.last__list-link(href='#') Главреды «Дождя», Forbes и других СМИ попросили Роскомнадзор разъяснить штрафы за ссылки на сайты с матом
          li.last__list-item.last__list-item--end
            img.last__list-image(src='/img/avatar-small-3.png' width='20' height='20' alt='Аватар пользователя')
            b.last__list-name Игорь Шманский
            a.last__list-link(href='#') Что-то все электрокары в последнее время все на одно лицо делаются))
    section.main-page__list.preview
      h2.visually-hidden Список превью статей
      ul.preview__list
        each article in articles
          li.preview__item
            ul.preview__breadcrumbs(style="flex-wrap: wrap")
              each category in article.categories
                li.preview__breadcrumbs-item
                  a.preview__breadcrumbs-link(href='#') #{category.name}
            .preview__background
              img.preview__background-image(src=`/media/img/${article.image}` width='460' height='240' srcset=`/media/img/${article.image}, /media/img/${article.image}` alt='Фотография небоскреба')
            time.preview__time(datetime=article.createDate) #{moment(article.createDate).format(`DD.MM.YYYY, hh:mm`)}
            h3.preview__name
              a.preview__name-link(href=`/articles/${article.id}`) #{article.title}
            p.preview__text #{article.announce}
            a.preview__comment(href='#')
              | Комментарии 
              span.preview__cloud
              |  
              b.preview__comment-count #{article.comments.length}
    .preview__toggle-wrapper
      ul.preview__pagination.pagination
        li
      if start > 1
        li
          a.pagination__button.button.button--backwards(href=`${prefix}page=${page - 1}` aria-label='Страница назад') Назад
      while i++ < end
        li
          a(class={active: i === page} href=`${prefix}page=${i}`)= i
      if end < totalPages
        li
          a.pagination__button.button.button--forward(href=`${prefix}page=${page + 1}` aria-label='Страница вперед') Вперёд

  +pagination(page, totalPages, 2, `?`)